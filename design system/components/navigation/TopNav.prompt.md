One-line: The desktop header — Glow Within wordmark on the left, sage-pill nav links on the right. Active link is a filled sage pill.

```jsx
<TopNav items={[{label:"Today",active:true},{label:"Garden"},{label:"Intentions"},{label:"Vision Board"}]} />
<Wordmark size={20} />
```

`Wordmark` is exported separately (amber "Gw" tile + serif name). No logo image ships in the source, so the brand is set in Fraunces.
