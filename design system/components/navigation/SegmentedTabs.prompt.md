One-line: A pill segmented control inside a pale track — the active segment fills sage with white text (e.g. Daily / Monthly intentions).

```jsx
<SegmentedTabs options={["Daily intentions","Monthly intentions"]} onChange={setTab} />
```

Uncontrolled by default; pass `value` + `onChange` to control it.
