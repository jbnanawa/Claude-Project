One-line: Rounded text field with a soft sage focus ring — the standard single-line input across Glow Within forms.

```jsx
<Input placeholder="e.g. Morning light through the window" />
```

Pairs with a plain `<label>` styled at 14px/500 in `--text-secondary`. Focus lights the border sage plus a translucent ring.
